# Имя org/user — используется для pull из ghcr.io
GITHUB_ORG=your-github-org

# PostgreSQL
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb

# Теги образов — обновляются CI автоматически
FRONTEND_TAG=latest
BACKEND_TAG=latest
